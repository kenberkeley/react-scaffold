/**
 * xhr interface definition
 * you can implement it with jQuery.ajax / axios / superagent / fetch ...
 * 
 * xhr({ method, url, body })
 * @param  {String}  options.method
 * @param  {String}  options.url
 * @param  {Object}  options.body
 * @return {Promise}
 */
export default from './jquery'
